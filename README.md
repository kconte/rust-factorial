# `factorial` Rust crate
Convenience crate that provides an implementation for the factorial (!) operator
for rust integer primitives.

Use `num-bigint` feature for `BigInt` and `BigUint` implmentations.
